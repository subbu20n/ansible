- name: install nginx and run
  hosts: frontend
  tasks:
  - name: install nginx
    ansible.builtin.package:
     name: nginx
     state: present
  - name: start nginx
    ansible.builtin.service:
     name: nginx
     state: present
     enabled: yes   