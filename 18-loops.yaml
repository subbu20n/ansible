- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.debug:
     name: "{{ item }}"
     state: installed
    loops:
    - mysql
    - nginx
    - zip  