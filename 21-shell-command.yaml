- name: shell vs command 
  hosts: frontend 
  become: yes 
  tasks: 
  - name: redirect to output to a file
    ansible.builtin.shell: "ls -ltr > /tmp/output.log"                                                                