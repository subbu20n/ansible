- name: user creation 
  hosts: local
  connection: local 
  tasks:
  - name: check roboshop user exixt or not 
    ansible.builtin.command: id roboshop
    register: output
    ignore_errors: true  
       